#ifndef DISPLAY_H
#define DISPLAY_H

#include <iostream>

using namespace std;

ostream& bold_on(ostream& os);
ostream& bold_off(ostream& os);
ostream& underline_on(ostream& os);
ostream& underline_off(ostream& os);


#endif